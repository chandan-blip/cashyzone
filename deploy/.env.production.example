# Copy to  app/.env  on the VPS and fill in real values.
PORT=3100

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=cashy
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DB_NAME=cashyzone

# Long random string — generate with:  openssl rand -hex 32
JWT_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_STRING
